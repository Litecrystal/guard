module.exports = {
    defaultPrefix: 'g!',
    ownerID: '538809111471587359',
    _limits: 'The following are defaults.',
    adminCanChangeLimits: true,
    limits: {
        user_removals: {
            per_minute: 8,
            per_hour: 24
        },
        role_creations: {
            per_minute: 4,
            per_hour: 12
        },
        channel_creations: {
            per_minute: 4,
            per_hour: 12
        },
        role_deletions: {
            per_minute: 4,
            per_hour: 12
        },
        channel_deletions: {
            per_minute: 4,
            per_hour: 12
        },
        unbans: {
            per_minute: 8,
            per_hour: 24
        }
    },
    _config: 'The following are defaults.',
    config: {
        _null: 'No options to configure currently.'
    }
};
